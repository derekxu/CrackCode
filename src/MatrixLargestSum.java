
public class MatrixLargestSum {
	public static int getMaxMatrix(int[][] original){
		int maxArea = Integer.MIN_VALUE;
		int rowCount = original.length;
		int colCount = original[0].length;
		int[][] matrix = precomputeMatrix(original);
	}
}
